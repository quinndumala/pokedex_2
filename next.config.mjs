/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : [
            "localhost",
            "assets.pokemon.com",
            "images.pokemontcg.io",
            "raw.githubusercontent.com",
        ]
      }
};

export default nextConfig;
